# Production reverse proxy. Replace evernest.example.com with your domain.
{
	email admin@example.com
}

evernest.example.com {
	encode gzip zstd

	@api path /v1/* /healthz /readyz
	handle @api {
		reverse_proxy api:8080
	}

	handle {
		reverse_proxy web:80
	}
}
