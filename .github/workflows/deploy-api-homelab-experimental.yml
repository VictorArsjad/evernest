name: deploy-api-homelab-experimental

# EXPERIMENTAL — homelab deploy via Portainer API (Option B).
#
# Does NOT replace deploy-api.yml (SSH prod path). Off by default until you set
# repo variable HOMELAB_DEPLOY_ENABLED=true and configure the PORTAINER_* secrets.
#
# Flow: CI green → build/push GHCR → Tailscale join → Portainer git/redeploy
# (pulls latest compose from Git + repulls :latest API image).
#
# One-time Portainer setup (then CI-only): docs/homelab-ci-experimental.md

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["ci"]
    branches: [master]
    types: [completed]

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: deploy-api-homelab-experimental
  cancel-in-progress: false

jobs:
  build-push:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success' &&
       vars.HOMELAB_DEPLOY_ENABLED == 'true')
    name: build + push image
    runs-on: ubuntu-latest
    outputs:
      sha_tag: ${{ steps.meta.outputs.sha_tag }}
      image: ${{ steps.meta.outputs.image }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: derive tags
        id: meta
        run: |
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${OWNER_LC}/evernest-api"
          SHA="${{ github.event.workflow_run.head_sha || github.sha }}"
          SHORT="${SHA:0:7}"
          {
            echo "image=${IMAGE}"
            echo "sha_tag=${SHORT}"
            echo "tags=${IMAGE}:latest,${IMAGE}:${SHORT}"
          } >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - name: log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: build + push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: infra/docker/api.Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-portainer:
    if: >-
      always() &&
      needs.build-push.result == 'success' &&
      (github.event_name == 'workflow_dispatch' ||
       vars.HOMELAB_DEPLOY_ENABLED == 'true')
    name: redeploy via Portainer API
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - name: tailscale up (ephemeral)
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          hostname: gh-homelab-${{ github.run_id }}

      - name: portainer git redeploy
        env:
          PORTAINER_URL: ${{ secrets.PORTAINER_URL }}
          PORTAINER_API_KEY: ${{ secrets.PORTAINER_API_KEY }}
          PORTAINER_STACK_ID: ${{ secrets.PORTAINER_STACK_ID }}
          PORTAINER_ENDPOINT_ID: ${{ secrets.PORTAINER_ENDPOINT_ID }}
          PORTAINER_GIT_TOKEN: ${{ secrets.PORTAINER_GIT_TOKEN }}
          DEPLOYED_SHA: ${{ needs.build-push.outputs.sha_tag }}
        run: |
          set -euo pipefail
          for v in PORTAINER_URL PORTAINER_API_KEY PORTAINER_STACK_ID PORTAINER_ENDPOINT_ID; do
            if [ -z "${!v:-}" ]; then
              echo "::error::$v secret is not set — see docs/homelab-ci-experimental.md"
              exit 1
            fi
          done

          if [ -n "${PORTAINER_GIT_TOKEN:-}" ]; then
            BODY=$(jq -n \
              --arg user "${{ github.repository_owner }}" \
              --arg pass "$PORTAINER_GIT_TOKEN" \
              '{
                repullImageAndRedeploy: true,
                repositoryAuthentication: true,
                repositoryUsername: $user,
                repositoryPassword: $pass
              }')
          else
            BODY='{"repullImageAndRedeploy": true, "repositoryAuthentication": false}'
          fi

          # -k: Portainer ships with a self-signed TLS cert by default. We're
          # already on the tailnet (ephemeral runner joined above) so the
          # transport is encrypted end-to-end; skip CA validation rather than
          # stand up cert-fronting just for this one PUT. Revisit if/when
          # Portainer gets a real cert (e.g. Tailscale serve in front).
          curl -fsSLk -X PUT \
            "${PORTAINER_URL%/}/api/stacks/${PORTAINER_STACK_ID}/git/redeploy?endpointId=${PORTAINER_ENDPOINT_ID}" \
            -H "X-API-Key: ${PORTAINER_API_KEY}" \
            -H "Content-Type: application/json" \
            -d "$BODY"

          echo "Portainer redeploy triggered for image tag ${DEPLOYED_SHA} (:latest on stack)"
